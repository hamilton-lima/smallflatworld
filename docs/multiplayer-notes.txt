- change realm data structures from array to Map
  - update fix
  - update persistence code 
- listen to the first update and rebuild the realm, then change to listen to all events 
  onStateChange.once(callback)
  


StateUpdate -> Realm ?


export class StateUpdate implements ClientData, ClientResponseData {
    data: SceneElementMemento[];
    images: SceneImage[];
    audios: SceneAudio[];
    codes: SceneCode[];
    designs3D: SceneDesign3D[];
  }

  export class Realm {
    id: string;
    name: string;
    character: SceneElementMemento;
    elements: SceneElementMemento[];
    images: SceneImage[];
    audios: SceneAudio[];
    codes: SceneCode[];
    designs3D: SceneDesign3D[];
  }