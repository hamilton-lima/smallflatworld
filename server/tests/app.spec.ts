// force full coverage check
import { suite, test } from "@testdeck/mocha";
import { expect } from "chai";

@suite
export class AppUnitTests {
  @test "server should exis TBD"() {
    expect(true).to.be.true;
  }
}
