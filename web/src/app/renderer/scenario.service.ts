import { Injectable } from '@angular/core';
import {
  Scene,
  Mesh,
  Color3,
  StandardMaterial,
  MeshBuilder,
  FollowCamera,
  Camera,
  Vector3,
} from '@babylonjs/core';

@Injectable({
  providedIn: 'root',
})
export class ScenarioService {

  constructor() {}
}
