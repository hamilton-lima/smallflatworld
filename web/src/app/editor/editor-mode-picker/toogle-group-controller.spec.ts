import { ToggleGroupController } from './toogle-group-controller';

describe('ToggleGroupController', () => {

  it('should be created', () => {
    const instance = new ToggleGroupController();
    expect(instance).toBeTruthy();
  });
});
